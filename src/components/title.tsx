import React from 'react';

export default class Title extends React.Component{
    render(){
        return(
            <header className={"title"}>
                This is a fake messenger app.
            </header>
        );
    }
}